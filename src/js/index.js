import '../css/all.css'
import '../css/home.scss'
import '../css/about.sass'

import '../index.html'
import '../img/web.jpeg'

console.log("Hello Webpack !")
console.log("edit me, use watch script can keep :D")

const foodList = ['apple', 'cake', 'chocolate', 'cookie']
foodList.forEach((item, i) => {
  console.log(`${i} - ${item}`)
})